diff --git a/.vscode/settings.json b/.vscode/settings.json
new file mode 100644
index 0000000..082b194
--- /dev/null
+++ b/.vscode/settings.json
@@ -0,0 +1,3 @@
+{
+    "makefile.configureOnOpen": false
+}
\ No newline at end of file
diff --git a/Makefile b/Makefile
index b262c0a..e3b1392 100644
--- a/Makefile
+++ b/Makefile
@@ -145,6 +145,7 @@ UPROGS=\
 	$U/_logstress\
 	$U/_forphan\
 	$U/_dorphan\
+	$U/_test_scheduler\
 
 fs.img: mkfs/mkfs README $(UPROGS)
 	mkfs/mkfs fs.img README $(UPROGS)
@@ -166,7 +167,7 @@ QEMUGDB = $(shell if $(QEMU) -help | grep -q '^-gdb'; \
 	then echo "-gdb tcp::$(GDBPORT)"; \
 	else echo "-s -p $(GDBPORT)"; fi)
 ifndef CPUS
-CPUS := 3
+CPUS := 1
 endif
 
 QEMUOPTS = -machine virt -bios none -kernel $K/kernel -m 128M -smp $(CPUS) -nographic
diff --git a/kernel/proc.c b/kernel/proc.c
index 22a5401..0b2528b 100644
--- a/kernel/proc.c
+++ b/kernel/proc.c
@@ -6,6 +6,7 @@
 #include "proc.h"
 #include "defs.h"
 
+
 struct cpu cpus[NCPU];
 
 struct proc proc[NPROC];
@@ -26,6 +27,28 @@ extern char trampoline[]; // trampoline.S
 // must be acquired before any p->lock.
 struct spinlock wait_lock;
 
+int
+do_rand(unsigned long *ctx)
+{
+  long hi, lo, x;
+  x = (*ctx % 0x7ffffffe) + 1;
+  hi = x / 127773;
+  lo = x % 127773;
+  x = 16807 * lo - 2836 * hi;
+  if (x < 0)
+  x += 0x7fffffff;
+  x--;
+  *ctx = x;
+  return (x);
+}
+unsigned long rand_next = 1;
+int
+rand(void)
+{
+return (do_rand(&rand_next));
+}
+
+
 // Allocate a page for each process's kernel stack.
 // Map it high in memory, followed by an invalid
 // guard page.
@@ -124,7 +147,8 @@ allocproc(void)
 found:
   p->pid = allocpid();
   p->state = USED;
-
+  p->tickets = 0;
+  p->turns = 0;
   // Allocate a trapframe page.
   if((p->trapframe = (struct trapframe *)kalloc()) == 0){
     freeproc(p);
@@ -228,6 +252,8 @@ userinit(void)
 
   p->state = RUNNABLE;
 
+  p->tickets = 10;
+
   release(&p->lock);
 }
 
@@ -268,6 +294,9 @@ kfork(void)
     return -1;
   }
 
+  np->tickets=p->tickets;
+  np->turns=0;
+
   // Copy user memory from parent to child.
   if(uvmcopy(p->pagetable, np->pagetable, p->sz) < 0){
     freeproc(np);
@@ -421,10 +450,11 @@ kwait(uint64 addr)
 //  - swtch to start running that process.
 //  - eventually that process transfers control
 //    via swtch back to the scheduler.
+
 void
 scheduler(void)
 {
-  struct proc *p;
+  // struct proc *p;
   struct cpu *c = mycpu();
 
   c->proc = 0;
@@ -436,32 +466,44 @@ scheduler(void)
     // and wfi.
     intr_on();
     intr_off();
-
-    int found = 0;
-    for(p = proc; p < &proc[NPROC]; p++) {
+    int total_tickets=0;
+    for(struct proc *p = proc; p < &proc[NPROC]; p++){
       acquire(&p->lock);
-      if(p->state == RUNNABLE) {
-        // Switch to chosen process.  It is the process's job
-        // to release its lock and then reacquire it
-        // before jumping back to us.
-        p->state = RUNNING;
-        c->proc = p;
-        swtch(&c->context, &p->context);
-
-        // Process is done running for now.
-        // It should have changed its p->state before coming back.
-        c->proc = 0;
-        found = 1;
+      if(p->state == RUNNABLE){
+        total_tickets+=p->tickets;
       }
       release(&p->lock);
     }
-    if(found == 0) {
-      // nothing to run; stop running on this core until an interrupt.
+
+    if(total_tickets == 0) {
       asm volatile("wfi");
+      continue;
     }
+
+    int lottery_result=rand()%total_tickets;
+    int starting=0;
+    
+    for(struct proc *p = proc; p < &proc[NPROC]; p++){
+      acquire(&p->lock);
+      if(p->state==RUNNABLE){
+        if(lottery_result>=starting && lottery_result<(starting+(p->tickets))){
+          p->turns++;
+          p->state=RUNNING;
+          c->proc=p;
+          swtch(&c->context, &p->context);
+          c->proc=0;
+          release(&p->lock);
+          break;
+        }
+        starting+=p->tickets;
+      }
+      release(&p->lock);
+    }
+    
   }
 }
 
+
 // Switch to scheduler.  Must hold only p->lock
 // and have changed proc->state. Saves and restores
 // intena because intena is a property of this
@@ -684,7 +726,7 @@ procdump(void)
       state = states[p->state];
     else
       state = "???";
-    printf("%d %s %s", p->pid, state, p->name);
+    printf("%d %s %s %d", p->pid, state, p->name, p->turns);
     printf("\n");
   }
 }
diff --git a/kernel/proc.h b/kernel/proc.h
index d021857..b1fd651 100644
--- a/kernel/proc.h
+++ b/kernel/proc.h
@@ -104,4 +104,6 @@ struct proc {
   struct file *ofile[NOFILE];  // Open files
   struct inode *cwd;           // Current directory
   char name[16];               // Process name (debugging)
+  int tickets;
+  int turns;
 };
diff --git a/kernel/syscall.c b/kernel/syscall.c
index 076d965..e8daf8c 100644
--- a/kernel/syscall.c
+++ b/kernel/syscall.c
@@ -101,6 +101,7 @@ extern uint64 sys_unlink(void);
 extern uint64 sys_link(void);
 extern uint64 sys_mkdir(void);
 extern uint64 sys_close(void);
+extern uint64 sys_settickets(void);
 
 // An array mapping syscall numbers from syscall.h
 // to the function that handles the system call.
@@ -126,6 +127,7 @@ static uint64 (*syscalls[])(void) = {
 [SYS_link]    sys_link,
 [SYS_mkdir]   sys_mkdir,
 [SYS_close]   sys_close,
+[SYS_settickets] sys_settickets,
 };
 
 void
diff --git a/kernel/syscall.h b/kernel/syscall.h
index 3dd926d..75a2424 100644
--- a/kernel/syscall.h
+++ b/kernel/syscall.h
@@ -20,3 +20,4 @@
 #define SYS_link   19
 #define SYS_mkdir  20
 #define SYS_close  21
+#define SYS_settickets  22
diff --git a/kernel/sysproc.c b/kernel/sysproc.c
index 419e727..9a3bf07 100644
--- a/kernel/sysproc.c
+++ b/kernel/sysproc.c
@@ -7,6 +7,23 @@
 #include "proc.h"
 #include "vm.h"
 
+uint64
+sys_settickets(void)
+{
+  int tick;
+  argint(0, &tick);
+
+  if(tick < 1)
+    return -1;
+
+  struct proc *p = myproc();
+  acquire(&p->lock);
+  p->tickets = tick;
+  release(&p->lock);
+
+  return 0;
+}
+
 uint64
 sys_exit(void)
 {
diff --git a/user/test_scheduler.c b/user/test_scheduler.c
new file mode 100644
index 0000000..6068d68
--- /dev/null
+++ b/user/test_scheduler.c
@@ -0,0 +1,10 @@
+#include "kernel/types.h"
+#include "user/user.h"
+
+int main(int argc, char *argv[]) {
+  int t = atoi(argv[1]);
+  settickets(t);
+  while(1) {}
+  return 0;
+}
+
diff --git a/user/user.h b/user/user.h
index ac84de9..4c6be50 100644
--- a/user/user.h
+++ b/user/user.h
@@ -21,6 +21,7 @@ int mkdir(const char*);
 int chdir(const char*);
 int dup(int);
 int getpid(void);
+int settickets(int);
 char* sys_sbrk(int,int);
 int pause(int);
 int uptime(void);
diff --git a/user/usys.pl b/user/usys.pl
index c5d4c3a..087dd83 100755
--- a/user/usys.pl
+++ b/user/usys.pl
@@ -42,3 +42,4 @@ entry("getpid");
 entry("sbrk");
 entry("pause");
 entry("uptime");
+entry("settickets");
